p-class S {
    var x : Int
    var print : Boolean

    constructor initially() {
        transit:
            x := 0
            print := true
    }
    
    transition increment(inc : Int) {
        guard:
            (x <= 10) & (inc > 0) & (print = false)
        transit:
            x := x + inc
            print := true
    }

    transition println(msg : String) {
        guard:
            (print = true) & (msg = x + "")
        transit:
            print := false
    }
}

p-class T {
    var x : Int

    constructor initially() {
        transit:
            x := 0
    }

    transition increment(inc : Int) {
        guard:
            inc > 3 & x <= 10
        transit:
            x := x + inc
    }

    transition println(msg : String) {
        guard:
            (x > 1 & x <= 10) & (msg = x + "")
        transit:
            x := x + 1
    }
}

program TermTest2 := S || T || Println
