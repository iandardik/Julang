# Raft cluster membership (static).
# - One absolute node URL per line: scheme://host:port  (no path)
# - Lines starting with # are comments; blank lines ignored
# - Line order defines stable node ids 0 .. n-1
# - Majority quorum is (n/2)+1 for the number of URLs listed
http://127.0.0.1:5001
http://127.0.0.1:5002
http://127.0.0.1:5003
