source: regression/input/imports/qualified-proc-expr.jul
programs:
  - name: QualifiedProcExprTest
    run:
      # Foreground: Driver exitProcess()s after "ok". Avoids background
      # durationMs being capped below the 1s timer when compile is slow.
      timeoutMs: 15000
      expectStdout: regression/expected/imports-qualified-proc-expr.stdout
