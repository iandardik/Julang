source: regression/input/sync-opt/unilateral-arg-owner.jul
tags: [sync-opt, sync-opt-parity]
compileTwiceWithDisableOpt: true
programs:
  - name: UnilateralArgOwner
    run:
      timeoutMs: 60000
      expectStdoutContains:
        - "hub 3"
