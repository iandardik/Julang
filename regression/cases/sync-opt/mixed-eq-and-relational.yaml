source: regression/input/sync-opt/mixed-eq-and-relational.jul
tags: [sync-opt, sync-opt-parity]
compileTwiceWithDisableOpt: true
programs:
  - name: MixedEqAndRelational
    run:
      timeoutMs: 60000
      expectStdoutContains:
        - "Q 2 5"
