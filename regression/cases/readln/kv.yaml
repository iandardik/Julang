source: regression/input/readln/kv.jul
programs:
  - name: KVOne
    run:
      timeoutMs: 30000
      stdin:
        - "A"
        - "B"
        - "C"
        - "exit"
      expectStdout: regression/expected/readln-kv-one.stdout
  - name: KVTwo
    run:
      timeoutMs: 30000
      stdin:
        - "A"
        - "B"
        - "C"
        - "exit"
      expectStdout: regression/expected/readln-kv-two.stdout
