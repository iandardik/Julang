type Point {
    x : Int
    y : Int
}

proc Bad {
    var pos : Point

    constructor initially(args : List<String>) {
        transit:
            pos := Point { x := 0 }
    }
}

proc BadTest := Bad

compile BadTest
