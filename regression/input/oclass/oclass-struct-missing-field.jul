o-class Point {
    x : Int
    y : Int
}

p-class Bad {
    var pos : Point

    constructor initially(args : List String) {
        transit:
            pos := Point { x := 0 }
    }
}

program BadTest := Bad
