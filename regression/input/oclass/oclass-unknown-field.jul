type Point {
    x : Int
}

proc Bad {
    var pos : Point

    constructor initially(args : List<String>) {
        transit:
            pos := Point { x := 0 }
    }

    transition check() {
        guard:
            pos.z = 0
    }
}

proc BadTest := Bad

compile BadTest
