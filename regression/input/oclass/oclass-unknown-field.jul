o-class Point {
    x : Int
}

p-class Bad {
    var pos : Point

    constructor initially(args : List<String>) {
        transit:
            pos := Point { x := 0 }
    }

    transition check() {
        guard:
            pos.z = 0
    }
}

program BadTest := Bad
