o-class Point {
    x : Int
}

p-class Bad {
    var pos : Point

    constructor initially(p : Point) {
        transit:
            pos := p
    }

    transition check() {
        guard:
            pos.z = 0
    }
}

program BadTest := Bad
