import julaylib.pclass.Println
import julaylib.pclass.Println

p-class S {
    var x : Int

    constructor initially(args : List<String>) {
        transit: x := 0
    }

    transition println(msg : String) {
        guard: msg = x + ""
    }
}

program ImportDuplicateTest := S || Println
