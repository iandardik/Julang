p-class S {
    var x : Int

    constructor initially(args : List<String>) {
        transit: x := 0
    }

    transition println(msg : String) {
        guard: msg = x + ""
    }
}

program ImportMissingTest := S || Println
