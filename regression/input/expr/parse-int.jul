import julaylib.pclass.Println
import julaylib.pclass.ExitSystem
import julaylib.fun.parseInt

p-class ParseIntDemo {
    var n : Int
    var print : Boolean

    constructor initially(args : List String) {
        transit:
            n := parseInt(args[0])
            print := true
    }

    p2p-consumer transition println(msg : String) {
        guard:
            print & (msg = n + "")
        transit:
            print := false
    }

    p2p-consumer transition exitSystem() {
        guard:
            ~print & n = 42
    }
}

program ParseIntTest := ParseIntDemo || Println || ExitSystem
