import julaylib.pclass.Println
import julaylib.pclass.ExitSystem
import julaylib.fun.parseInt

proc ParseIntDemo {
    const n : Int
    var print : Boolean

    constructor initially(args : List<String>) {
        transit:
            n := parseInt(args[0])
            print := true
    }

    transition println(msg : String) {
        guard:
            print & (msg = n + "")
        transit:
            print := false
    }

    transition exitSystem() {
        guard:
            ~print & n = 42
    }
}

proc ParseIntTest := ParseIntDemo || Println || ExitSystem

compile ParseIntTest
