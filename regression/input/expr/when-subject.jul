import julay.proclib.Println
import julay.proclib.ExitSystem

proc S {
    var key : String
    var x : Int
    var print : Boolean
    var numAssign : Int

    constructor initially(args : List<String>) {
        transit:
            key := "B"
            x := 0
            print := true
            numAssign := 0
    }

    internal transition assign() {
        guard:
            print & numAssign < 10
        transit:
            x := when (key) {
                "A" -> 1
                "B" -> 2
                else -> 0
            }
            print := false
            numAssign := numAssign + 1
    }

    transition println(msg : String) {
        guard:
            ~print & (msg = x + "")
        transit:
            print := true
    }

    transition exitSystem() {
        guard:
            print
    }
}

proc P := S || Println || ExitSystem

compile P
