import julaylib.pclass.Println
import julaylib.pclass.ExitSystem

proc S {
    var x : Int
    var nested : List<List<Int>>
    var print : Boolean

    constructor initially(args : List<String>) {
        transit:
            x := let (inc : Int := 2) { inc + 3 }
            nested := let (ys : List<List<Int>> := [[1], [2, 3]]) { ys }
            print := true
    }

    transition println(msg : String) {
        guard:
            print & (msg = x + "") & nested[1][0] = 2
        transit:
            print := false
    }

    transition exitSystem() {
        guard:
            ~print
    }
}

proc P := S || Println || ExitSystem

compile P
