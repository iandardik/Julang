import julay.funlib.listOf
proc ListIndexAssignBadIndex {
    var xs : List<Int>

    constructor initially(args : List<String>) {
        transit:
            xs := listOf(1, 2)
            xs[1.0] := 3
    }
}

compile ListIndexAssignBadIndex
