import julay.funlib.map
import julay.funlib.listOf

fun add(a : Int, b : Int) : Int = a + b

proc MapBadFun {
    var xs : List<Int>

    constructor initially(args : List<String>) {
        transit:
            xs := map(listOf(1, 2), add)
    }
}

compile MapBadFun
