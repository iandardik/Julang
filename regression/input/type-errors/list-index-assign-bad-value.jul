proc ListIndexAssignBadValue {
    var xs : List<Int>

    constructor initially(args : List<String>) {
        transit:
            xs := [1, 2]
            xs[0] := true
    }
}

compile ListIndexAssignBadValue
