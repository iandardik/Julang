import julay.funlib.listOf
proc BadToSetArgs {
    var xs : List<Int>

    constructor initially(args : List<String>) {
        transit:
            xs := listOf(1).toSet(1)
    }
}

compile BadToSetArgs
