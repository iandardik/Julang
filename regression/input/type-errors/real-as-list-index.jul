import julay.funlib.listOf
proc S {
    var xs : List<Int>
    var n : Int

    constructor initially(args : List<String>) {
        transit:
            xs := listOf(1, 2, 3)
            n := xs[1.0]
    }
}

proc BadIndex := S

compile BadIndex
