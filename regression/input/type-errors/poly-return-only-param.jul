o-class Box<T> {
    value : T
}

fun const<T>(x : Int) : T = x

p-class Worker {
    var n : Int

    constructor initially(args : List<String>) {
        transit:
            n := 0
    }
}

program PolyReturnOnlyParamTest := Worker
