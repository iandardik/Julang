obj Box<T> {
    value : T
}

fun constFn<T>(x : Int) : T = x

proc Worker {
    var n : Int

    constructor initially(args : List<String>) {
        transit:
            n := 0
    }
}

proc PolyReturnOnlyParamTest := Worker

compile PolyReturnOnlyParamTest
