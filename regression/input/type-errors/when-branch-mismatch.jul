proc S {
    var x : Int

    constructor initially(args : List<String>) {
        transit:
            x := when (1) {
                1 -> 0
                else -> false
            }
    }
}

proc P := S

compile P
