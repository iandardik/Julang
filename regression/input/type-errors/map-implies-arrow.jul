import julay.funlib.listOf
proc Worker {
    var mp : Map<String, Int>

    constructor initially(args : List<String>) {
        transit:
            mp := listOf("a" => 1)
    }
}

proc WorkerMain := Worker

compile WorkerMain
