obj Box<T> {
    value : T
}

proc Worker {
    var x : Box

    constructor initially(args : List<String>) {
        transit:
            x := Box<Int> { value := 1 }
    }
}

proc PolyBareParametricTest := Worker

compile PolyBareParametricTest
