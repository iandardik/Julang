import julay.funlib.listOf
proc HofLambdaArity {
    var xs : List<Int>

    constructor initially(args : List<String>) {
        transit:
            xs := listOf(1, 2).filter((a, b) -> a > 0)
    }
}

compile HofLambdaArity
