type Box<T, T> {
    value : T
}

proc Worker {
    var n : Int

    constructor initially(args : List<String>) {
        transit:
            n := 0
    }
}

proc PolyDupTypeParamTest := Worker

compile PolyDupTypeParamTest
