obj Box<T> {
    value : T
}

proc Worker {
    var box : Box<Int>

    constructor initially(args : List<String>) {
        transit:
            box := Box<Int> { value := "nope" }
    }
}

proc PolyLiteralFieldMismatchTest := Worker

compile PolyLiteralFieldMismatchTest
