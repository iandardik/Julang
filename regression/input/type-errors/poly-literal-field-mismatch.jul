o-class Box<T> {
    value : T
}

p-class Worker {
    var box : Box<Int>

    constructor initially(args : List<String>) {
        transit:
            box := Box<Int> { value := "nope" }
    }
}

program PolyLiteralFieldMismatchTest := Worker
