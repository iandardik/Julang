type Point {
    x : Int
    y : Int
}

proc S {
    var p : Point

    constructor initially(args : List<String>) {
        transit:
            p := let (n : Point := true) n
    }
}

proc P := S

compile P
