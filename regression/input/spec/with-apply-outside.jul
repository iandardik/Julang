type Node

proc Counter {
    var n : Int := 0

    constructor initially(args : List<String>) {
        transit: n := 0
    }

    transition bump() {
        transit: n := n + 1
    }
}

// Apply-index outside with is illegal.
spec BadApply := Counter[i]

compile BadApply
