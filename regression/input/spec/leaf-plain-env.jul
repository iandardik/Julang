spec Env {
    var ready : Boolean := false

    constructor initially(args : List<String>) {
        transit: ready := false
    }

    transition mark() {
        guard: ready | ~ready
        transit: ready := true
    }
}

spec LeafPlainEnv := Env

compile LeafPlainEnv
