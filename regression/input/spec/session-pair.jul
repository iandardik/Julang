proc Alice {
    var done : Boolean

    constructor initially() {
        transit: done := false
    }

    session transition meet() {
        guard: ~done
        transit: done := true
    }
}

proc Bob {
    var done : Boolean

    constructor initially() {
        transit: done := false
    }

    session transition meet() {
        guard: ~done
        transit: done := true
    }
}

invariant Inv := true

spec SessionPair := <true> Alice || Bob <Inv>
spec SessionAliceOnly := <true> Alice <Inv>

compile SessionPair, SessionAliceOnly
