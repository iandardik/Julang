type Node

type Inner {
    d : Node
}

type Outer {
    inner : Inner
}

proc Bad {
    var o : Outer
    constructor initially(args : List<String>) {
        transit: o := Outer { inner := Inner { d := "n1" } }
    }
}

compile Bad
