import julay.funlib.mapOf
proc BadBracket {
    var xs : List<Int>

    constructor initially(args : List<String>) {
        transit:
            xs := mapOf("a" to 1)
    }
}

proc P := BadBracket

compile P
