import julay.proclib.ExitSystem

proc BadMap {
    var mp : Map<String, Int>

    constructor initially(args : List<String>) {
        transit:
            mp := ["a" -> 1, "b" -> true]
    }

    transition exitSystem() {
        guard: true
    }
}

proc BadMapTest := BadMap || ExitSystem

compile BadMapTest
