p-class BadMapTransit {
    var mp : Map<String, Int>

    constructor initially(args : List<String>) {
        transit:
            mp := ["a" -> 1]
    }

    transition bad() {
        guard:
            true
        transit:
            mp := ["b" -> 2]
            mp["c"] := 3
    }
}

program P := BadMapTransit
