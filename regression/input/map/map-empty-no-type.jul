import julay.funlib.listOf
import julay.funlib.length
proc BadEmptyBracket {
    var n : Int

    constructor initially(args : List<String>) {
        transit:
            n := length(listOf())
    }
}

proc P := BadEmptyBracket

compile P
