proc S {
    const x : Int

    constructor initially(args : List<String>) {
        transit:
            x := 0
    }

    internal transition bump() {
        transit:
            x := x + 1
    }
}

proc P := S

compile P
