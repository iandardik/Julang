proc A {
    constructor initially(args : List<String>) {
        transit:
    }

    session transition ping() {
        guard: false
    }
}

proc B {
    constructor initially(args : List<String>) {
        transit:
    }

    transition ping() {
        guard: false
    }
}

proc P := A || B

compile P
