p-class S {
    var x : Int

    constructor initially(args : List<String>) {
        transit:
            x := 0
    }

    service transition println(msg : String) {
        guard:
            true
        transit:
            x := x + 1
    }
}

program P := S
