p-class S {
    var msg : String

    constructor initially(args : List String) {
        transit:
            msg := ""
    }

    transition go() {
        transit:
            msg := "pending"
        effect:
            msg := readln()
    }
}

program P := S
