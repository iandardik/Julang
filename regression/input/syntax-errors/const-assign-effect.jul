import julay.funlib.readln

proc S {
    const msg : String

    constructor initially(args : List<String>) {
        transit:
            msg := ""
    }

    internal transition read() {
        transit:
            msg := readln()
    }
}

proc P := S

compile P
