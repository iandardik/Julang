proc S {
    var x : Int

    constructor initially(args : List<String>) {
        guard:
            true
        transit:
            x := 0
    }

    internal transition done() {
        guard: true
        after: exitProcess()
    }
}

proc P := S

compile P
