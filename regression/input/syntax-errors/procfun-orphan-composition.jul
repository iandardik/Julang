procfun helper(x : Int) : Int {
    transition done() {
        return: x
    }
}

proc Other {
    constructor initially(args : List<String>) {
        transit:
    }
}

proc Bad := Other || helper

compile Bad
