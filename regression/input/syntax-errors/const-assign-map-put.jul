proc S {
    const mp : Map<String, Int>

    constructor initially(args : List<String>) {
        transit:
            mp := ["a" -> 1]
    }

    internal transition update() {
        transit:
            mp["b"] := 2
    }
}

proc P := S

compile P
