proc S {
    var x : Int

    constructor initially(args : List<String>) {
        transit:
            x := 0
    }

    provider transition println(msg : String) {
        guard:
            true
        transit:
            x := x + 1
    }
}

proc P := S

compile P
