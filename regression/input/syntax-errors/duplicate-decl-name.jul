proc S {
    var x : Int

    constructor initially(args : List<String>) {
        transit:
            x := 0
    }
}

proc S {
    var y : Int

    constructor initially(args : List<String>) {
        transit:
            y := 0
    }
}

proc P := S

compile P
