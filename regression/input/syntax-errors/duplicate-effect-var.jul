proc S {
    var x : String
    var y : Int

    constructor initially(args : List<String>) {
        transit:
            x := ""
            y := 0
    }

    internal transition go() {
        after:
            x := readln()
    }
}

proc P := S

compile P
