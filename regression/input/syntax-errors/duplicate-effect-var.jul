p-class S {
    var x : Int
    var y : Int

    constructor initially(args : List<String>) {
        transit:
            x := 0
            y := 0
    }

    internal transition go() {
        effect:
            x := readln()
            x := readln()
    }
}

program P := S
