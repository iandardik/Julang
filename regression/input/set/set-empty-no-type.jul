import julay.funlib.setOf
proc BadEmptySet {
    var n : Int

    constructor initially(args : List<String>) {
        transit:
            n := length(setOf())
    }
}

proc P := BadEmptySet

compile P
