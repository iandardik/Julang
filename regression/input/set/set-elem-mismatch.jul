import julaylib.pclass.ExitSystem

proc BadSet {
    var s : Set<Int>

    constructor initially(args : List<String>) {
        transit:
            s := {1, true}
    }

    transition exitSystem() {
        guard: true
    }
}

proc BadSetTest := BadSet || ExitSystem

compile BadSetTest
