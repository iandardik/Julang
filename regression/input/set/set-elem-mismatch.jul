import julay.funlib.exitProcess
import julay.funlib.setOf

proc BadSet {
    var s : Set<Int>

    constructor initially(args : List<String>) {
        transit:
            s := setOf(1, true)
    }

    internal transition exitSystem() {
        guard: true
        after:
            exitProcess()
    }
}

proc BadSetTest := BadSet

compile BadSetTest
