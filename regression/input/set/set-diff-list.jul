import julay.funlib.listOf
import julay.funlib.setOf
proc BadSetOp {
    var s : Set<Int>

    constructor initially(args : List<String>) {
        transit:
            s := setOf(1) - listOf(1)
    }
}

proc P := BadSetOp

compile P
