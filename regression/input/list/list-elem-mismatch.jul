import julay.funlib.exitProcess
import julay.funlib.listOf

proc BadList {
    var xs : List<Int>

    constructor initially(args : List<String>) {
        transit:
            xs := listOf(1, true)
    }

    internal transition exitSystem() {
        guard: true
        after:
            exitProcess()
    }
}

proc BadListTest := BadList

compile BadListTest
