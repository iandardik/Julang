import julay.proclib.ExitSystem

proc BadList {
    var xs : List<Int>

    constructor initially(args : List<String>) {
        transit:
            xs := [1, true]
    }

    transition exitSystem() {
        guard: true
    }
}

proc BadListTest := BadList || ExitSystem

compile BadListTest
