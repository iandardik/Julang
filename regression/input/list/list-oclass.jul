import julay.funlib.exitProgram
import julay.funlib.listOf

obj Point {
    x : Int
    y : Int
}

proc ListOClassDemo {
    var pts : List<Point>
    var done : Boolean

    constructor initially(args : List<String>) {
        transit:
            pts := listOf(Point { x := 1, y := 2 }, Point { x := 3, y := 4 })
            done := false
    }

    internal transition check() {
        guard:
            ~done & pts[1] = Point { x := 1, y := 2 } & pts[2] = Point { x := 3, y := 4 }
        transit:
            done := true
    }

    internal transition exitSystem() {
        guard:
            done
        after:
            exitProgram(0)
    }
}

proc ListOClassTest := ListOClassDemo

compile ListOClassTest
