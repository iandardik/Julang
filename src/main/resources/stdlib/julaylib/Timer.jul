p-class Timer {
    var done : Boolean

    constructor timer(time : Int) {
        transit: done := false
        effect:  delaySeconds(time)
    }

    transition timeout() {
        guard:   ~done
        transit: done := true
    }
}
